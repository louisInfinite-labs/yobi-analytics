# Environment / secrets
.env
.env.local
.env.*.local

# Web Push VAPID key material (Roadmap 4.6) — generated locally via py_vapid
vapid_private_key.pem
vapid_public_key.txt
env-dispatcher.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Claude Code local overrides
.claude/settings.local.json

# Locally collected data (regenerated from the YouTube API / rebuilt by discovery)
src/video_master.json
src/snapshots/

# Lambda deployment package (rebuilt by scripts/package_lambda.py)
build/
